---
  - hosts: localhost
    tasks:
       - debug: msg="Ansible is working!"
       - debug: msg="Wsl for Ansible is also working"
   - name: Set JAVA_HOME
     lineinfile:
       dest: /etc/environment
       state: present
       regexp: '^JAVA_HOME'
       line: 'JAVA_HOME=C:\Program Files\Java\jdk-11.0.15.1'
